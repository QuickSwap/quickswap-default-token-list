{
  "name": "quickswap-token-lists",
  "version": "1.3.81",
  "description": "◦ The Quickswap default token lists",
  "main": "build/quickswap-token-lists.json",
  "scripts": {
    "test": "mocha",
    "build": "rimraf build && mkdir -p build && node src/write.js > build/quickswap-token-lists.json",
    "prepublishOnly": "npm test && npm run build"
  },
  "files": [
    "build/quickswap-token-lists.json"
  ],
  "keywords": [
    "uniswap",
    "quickswap",
    "default",
    "token",
    "list"
  ],
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@ethersproject/address": "^5.0.2",
    "@uniswap/token-lists": "^1.0.0-beta.8",
    "ajv": "^6.12.3",
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "rimraf": "^3.0.2"
  }
}
